<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SwipecardsExemplo"
             xmlns:swipecards="clr-namespace:SwipeCards.Controls;assembly=SwipeCards.Controls"
             x:Class="SwipecardsExemplo.MainPage" Padding="0,20,0,0" BackgroundColor="#EEEEEE">


    <swipecards:CardStackView
            x:Name="CardStackView"
            ItemsSource="{Binding Cartoes}"
            Margin="20, 60">

        <swipecards:CardStackView.ItemTemplate>
            <DataTemplate>
                <Label Text="{Binding}" VerticalOptions="Center" HorizontalOptions="Center" />
            </DataTemplate>
        </swipecards:CardStackView.ItemTemplate>

    </swipecards:CardStackView> 

</ContentPage>
